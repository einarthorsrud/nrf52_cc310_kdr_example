# nrf52_cc310_kdr_example

Minimal example provisioning CC310 with the device root key (KDR) and then using key derivatin to extract a key.

Tested using nRF5 SDK 17.0.2, though the code itself should work with any nRF5 SDK version >= 15.0.0.
